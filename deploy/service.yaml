apiVersion: v1
kind: Service
metadata:
  name: mvcmovie
  labels:
    app: mvcmovie
    env: poc
    pic: wicaksono trihatmaja
spec:
  type: ClusterIP
  selector:
    app: mvcmovie
  ports:
  - port: 80
    targetPort: 8080
    protocol: TCP